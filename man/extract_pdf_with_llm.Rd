% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_llm_fct_extract.R
\name{extract_pdf_with_llm}
\alias{extract_pdf_with_llm}
\title{Core async function for LLM extraction}
\usage{
extract_pdf_with_llm(
  pdf_path,
  model_provider,
  model_name,
  env_var,
  chat_fn,
  api_key,
  params = NULL,
  extraction_prompt,
  extraction_schema,
  max_tokens,
  cache = "none"
)
}
\arguments{
\item{pdf_path}{Character. Path to the PDF file to extract data from.}

\item{model_provider}{Character. The provider of the LLM. One of Anthropic, OpenAI, or Google}

\item{model_name}{Character. The specific model to call (e.g. "claude-sonnet-5").}

\item{env_var}{Character. Name of the environment variable holding the API key
for this provider (set via \code{Sys.setenv()} before the call).}

\item{chat_fn}{Character. Name of the \code{ellmer} chat-constructor function to use
for this provider (e.g. "chat_anthropic").}

\item{api_key}{Character. Anthropic API key (should start with "sk-ant-").}

\item{params}{Function. Maximum tokens for the API response.}

\item{extraction_prompt}{Character. The prompt text instructing the LLM
on what to extract.}

\item{extraction_schema}{List or S7 class. The structured schema defining
expected output format (e.g., S7 class with properties).}

\item{max_tokens}{Integer. Maximum tokens for the API response.}

\item{cache}{Boolean Attempt to use ellmer's built in cache function. Disabled for calls to Google LLMs}
}
\value{
Named list with three elements:
\describe{
\item{result}{The structured extraction result from the LLM}
\item{metadata}{List containing cost information or error details}
\item{success}{Logical indicating if extraction succeeded}
}
}
\description{
This function performs the actual PDF extraction using the Anthropic API.
It's designed to run inside a mirai() call for async execution.
}
