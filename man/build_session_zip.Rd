% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_download.R
\name{build_session_zip}
\alias{build_session_zip}
\title{Build a session ZIP archive}
\usage{
build_session_zip(session, moduleState, dest_file)
}
\arguments{
\item{session}{Shiny session object. Required to access reactive values.}

\item{moduleState}{ReactiveValues object containing \code{available_datasets} and
\code{campaign_name} fields.}

\item{dest_file}{Character. Path where the ZIP file should be written.}
}
\value{
\code{dest_file} invisibly.
}
\description{
Writes all available session datasets to a ZIP file at \code{dest_file}.
Includes tabular data as CSV, text/object data as TXT, the source PDF (if
present), and a metadata file. Called by both \code{download_all_data()} and the
Zenodo module's session-upload mode.
}
\examples{
\dontrun{
  # session and moduleState are Shiny reactive objects
  build_session_zip(session, moduleState, dest_file = tempfile(fileext = ".zip"))
}
}
